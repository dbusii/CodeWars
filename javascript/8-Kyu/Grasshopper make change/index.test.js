describe('get change', () => {
  it('should define money and set value', () => {
    Test.assertEquals(money, 10)
  })
  it('should define candy and set value', () => {
    Test.assertEquals(candy, 1.42)
  })
  it('should define chips and set value', () => {
    Test.assertEquals(chips, 2.4)
  })
  it('should define soda and set value', () => {
    Test.assertEquals(soda, 1)
  })
})